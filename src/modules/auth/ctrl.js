const { loginModel } = require("./model");

const loginCtrl = async (req, res) => {
    try {
        // if(req.params)
    } catch (error) {
        console.log(error.message);
    }
}

module.exports = {
    loginCtrl
}